{
  "name": "runway-ml-app",
  "version": "1.0.0",
  "description": "AI Creative Media Platform with Text-to-Image",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": ["ai", "image-generation", "runway"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```

3. **Save it as `package.json`** (make sure it's NOT `package.json.txt`)
4. **Put it in your `runway-ml` folder** (same level as `server.js`)

---

### Option 2: Command Line (If you have Node.js installed)

1. Open terminal/command prompt in your `runway-ml` folder
2. Run: `npm init -y`
3. Then manually edit the file to add the dependencies from above

---

## 📁 Your Final Folder Structure Should Look Like:
```
runway-ml/
├── server.js          ← Create this (I gave you the code)
├── package.json       ← Create this (code above)
└── public/
    └── index.html     ← Your renamed HTML file